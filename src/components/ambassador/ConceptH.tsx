"use client";

import BercoSite from "./bercosite/BercoSite";

// H = Site "Berco Inc" — dark-mode eCommerce holding co landing page
// (bold-word headlines, scroll word-reveal statement, procedural chip + card stack).
export default function ConceptH() {
  return <BercoSite />;
}
