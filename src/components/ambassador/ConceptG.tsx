"use client";

import NgpesSite from "./ngpessite/NgpesSite";

// G = Site "NGPES" — light-mode French fintech payments infrastructure
// (orange-on-cream emphasis, mock product UIs, procedural globe + Eiffel scenes).
export default function ConceptG() {
  return <NgpesSite />;
}
