"use client";

import HeliosSite from "./heliossite/HeliosSite";

// F = Site B "HELIOS" (morphing particle system: galaxy / globe / terrain / map).
export default function ConceptF() {
  return <HeliosSite />;
}
